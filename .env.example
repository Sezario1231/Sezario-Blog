# ===== 公开变量 (客户端可访问) =====
# 前缀 PUBLIC_ 的变量会暴露给客户端
# 部署后替换为你的实际域名
PUBLIC_SITE_URL=http://localhost:4321
# PUBLIC_SITE_NAME=My Blog

# ===== 私有变量 (仅服务端，切勿提交到仓库或暴露前端) =====
# Supabase 连接配置（用于联系表单后端写入）
# 在 Supabase 控制台 Project Settings → API → API Keys 中复制
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# 说明：
# - PUBLIC_SITE_URL: 站点域名，用于 SEO sitemap 和 RSS 生成。
#                   本地开发填 http://localhost:4321，部署到 Vercel 后填正式域名。
# - SUPABASE_URL: Supabase 项目 URL
# - SUPABASE_SERVICE_ROLE_KEY: service_role 密钥，拥有完全权限且绕过 RLS，
#   仅在 src/lib/supabase.ts 服务端代码中使用，绝不可暴露给前端
# - 部署到 Vercel 时，在 Environment Variables 中配置同名变量